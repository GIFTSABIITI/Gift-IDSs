# Gift IDS Logs

Runtime logs are written here by default:

- `giftids.log` for packet events
- `giftids_alerts.log` for alerts

Log files are ignored by Git because they can contain sensitive network
metadata such as IP addresses, ports, timestamps, and traffic patterns.
