# PCAP Samples

Put small test PCAP files here when you want repeatable offline analysis.

Only use PCAP files from your own lab or trusted sources. Packet captures can
contain private network traffic, credentials, hostnames, and internal IPs, so do
not analyze private traffic without permission.

Example:

```sh
./giftids --read samples/test.pcap --stats
```
